var name = ['pasupathi', 'arun', 'arulmani', 'kumar'];

var pop = name.pop();//remove last element
console.log(name.join(' '));

var push = name.push('dinez');//add to last element
console.log(name.join(' '));

var shift = name.shift();//remove first element

console.log(name.join(' '));

var unshift = name.unshift('pasupathi');//add to first element

console.log(name.join(' '));